import CalculatorInterface from './interface';

describe('Interfacing with calculator', () => {
  test('Typing an operator is the same as clicking an operator', () => {

  });

  test('Typing an operator in combination with numerical inputs splits the numerical and operator part(s) and processes sequentially', () => {

  });
});
